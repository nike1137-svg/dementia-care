# 새록이 (dementia-care)

> 어르신을 위한 하루 3분 치매예방 웹앱 — 그리고 **개인 리눅스 데스크탑을 Docker로 서버화해 안전하게 배포**하는 학습 프로젝트

"기억이 새록새록"이라는 뜻의 **새록이**는 인지 정상 어르신이 매일 3분, 5단계로 두뇌를 쓰는 웹앱입니다. 다만 이 저장소의 진짜 목적은 앱 그 자체보다 **개인 컴퓨터를 컨테이너로 감싸 인터넷에 안전하게 노출하는 전 과정**을 직접 설계하고 검증하는 데 있습니다.

---

## 무엇을 만들었나

- 로그인 없이 **익명 UUID**로만 사용자를 구분 (개인정보 미수집)
- 하루 1회, **5단계 세션** (기분 → 준비운동 → 본운동 → 회상 → 미션)
- **정답 판정·난이도 조정을 서버에서만** 수행 (프런트는 정답을 모름)
- 연속 참여를 **도장판**으로 표시 (점수·정답률은 노출하지 않음 — 실패감 방지)
- 큰 글자·큰 버튼·넓은 간격 등 **어르신 UI 규격**을 토큰으로 관리
- **주차·일차 자동 순환** — 5일을 채우면 다음 주차로. 어디에도 저장하지 않고 완료 일수에서 파생
- **오답에는 정답이 아니라 단서** — 1차 오답 시 범주·속성 단서로 맞힐 확률을 올림 (errorless learning)
- **서버가 사용자를 잊어도 스스로 복구** — DB가 초기화돼 `401` 이 나면 새로 발급받아 다시 시작

---

## 아키텍처

```
[사용자]
   │  HTTPS
   ▼
[Cloudflare]  ── Access 인증(지정 사용자만) ──┐
   │  Tunnel (아웃바운드 전용)                 │
   ▼                                          │
┌─────────────── Docker (전용 네트워크) ───────────────┐
│  cloudflared ──▶ web (Next.js) ──▶ api (FastAPI)     │
│                                        │             │
│                                   SQLite (볼륨)       │
└──────────────────────────────────────────────────────┘
     ▲ 호스트에 열린 포트 없음 (ports 매핑 0개)
```

- **web** — Next.js. 화면을 그리고, `/api/py/*` 요청을 api로 중계(rewrite)
- **api** — FastAPI. 세션·판정·난이도·기록을 담당
- **SQLite** — 사용자·완료기록·진행상태를 영구 저장 (named volume)
- **cloudflared** — Cloudflare Tunnel 커넥터. 바깥으로 나가는 연결만 사용

---

## 진행 현황

| Phase | 내용 | 상태 |
|-------|------|------|
| 0 | 설계 문서 (PRD · 위협 모델 · API 명세) | ✅ 완료 |
| 1 | 목데이터로 화면 완성 (홈·세션·완료·기록) | ✅ 완료 |
| 2 | FastAPI 백엔드 — 서버가 판정·난이도 담당 | ✅ 완료 |
| 3 | 화면과 서버 실제 연결 (목데이터 → 실제 API) | ✅ 완료 |
| 4 | SQLite 연결 — 데이터 영구 저장 | ✅ 완료 |
| 5 | Docker Compose — 컨테이너 격리 | ✅ 완료 |
| 6 | Cloudflare Tunnel — 인터넷 노출 + Access | ✅ 완료 |
| 7 | README · 마무리 문서 | ✅ 완료 |

**Docker 서버화라는 이 저장소의 목표는 Phase 7에서 완료됐습니다.** 이후 작업은 앱 골격이 아니라 아래 콘텐츠 커리큘럼을 채우는 일입니다.

---

## 콘텐츠 커리큘럼 (12주)

주 5일(월~금) · 6영역 × 2주 = 1사이클 · 13주차부터 난이도를 올려 재순환합니다 (`docs/prd.md` §3.2).

주차·일차는 어디에도 저장하지 않고 **완료한 일수에서 매번 파생**하므로, 콘텐츠 파일만 추가하면 새 주차가 자동으로 인식됩니다 (`docs/api-spec.md` §3.1).

| 주차 | 인지 영역 | 문항 작성 |
|------|-----------|-----------|
| 1~2 | 지남력 | ✅ |
| 3~4 | 주의·집중 | ✅ |
| 5 | 기억력 — 단서 회상 (Spaced Retrieval) | ✅ |
| 6 | 기억력 (2주차) | ⬜ |
| 7~8 | 언어 | ⬜ |
| 9~10 | 계산·실행 | ⬜ |
| 11~12 | 시공간·회상 | ⬜ |

> 위 표는 **문항 파일 작성 여부**입니다. 각 주차가 운영 서버에 배포됐는지는 `docs/handoff.md` §1을 봅니다.

새 주차를 추가하면 검증기로 형식·정답 유출·인출 일정 정합성을 검사합니다. 하나라도 걸리면 종료 코드 1입니다.

```bash
python3 scripts/validate_content.py            # 전체
python3 scripts/validate_content.py --week 5   # 한 주차만
```

---

## 기술 스택

| 구분 | 사용 기술 |
|------|-----------|
| 프런트엔드 | Next.js (TypeScript) |
| 백엔드 | FastAPI (Python) |
| 데이터베이스 | SQLite |
| 컨테이너 | Docker, Docker Compose |
| 배포·네트워크 | Cloudflare Tunnel, Cloudflare Access |

모든 구성 요소는 무료 범위 안에서 운용합니다.

---

## 실행 방법

> 로컬 개발과 컨테이너 실행이 분리되어 있습니다. 비밀값은 저장소에 포함되지 않으며, 각자 환경에서 설정해야 합니다.

### 사전 준비

- Docker / Docker Compose
- Cloudflare 계정 및 관리 중인 도메인 (터널·Access용)
- 터널 토큰 등 비밀값은 `.env`에 직접 설정 (저장소에 미포함, `.gitignore` 처리됨)

### 컨테이너 실행

```bash
# api·web 빌드
sudo docker compose build api web

# 터널 포함 전체 기동 (.env의 TUNNEL_TOKEN 필요)
sudo docker compose --profile tunnel up -d

# 상태 확인
sudo docker compose ps

# 정리 (데이터 볼륨은 유지)
sudo docker compose down
```

> 실행 환경에 따라 `sudo` 여부가 다를 수 있습니다.

### 로컬 데모 (개발용)

화면을 보며 **아무 주차나 즉시 확인**하기 위한 도구입니다. 도커를 쓰지 않아
`sudo` 가 필요 없고, 운영 컨테이너·운영 DB와 완전히 분리됩니다
(별도 파일 `api/data/demo.db`, localhost 전용, 터널 없음).

```bash
./scripts/demo.sh
```

띄운 뒤 브라우저로 `http://localhost:3000` 을 한 번 열어 사용자를 발급받고,
**다른 터미널**에서 원하는 지점으로 건너뜁니다.

```bash
python3 scripts/demo_jump.py --week 4 --day 2 --level 3
```

새로고침하면 바로 그 화면이 나옵니다. 주차·일차는 완료 일수(`N`)에서 파생되므로
(`docs/api-spec.md` §3.1) 원래는 4주차를 보려면 16일을 기다려야 합니다 — 이 스크립트가
과거 완료 기록을 채워 그 지점으로 옮깁니다. **오늘 날짜는 비워 두므로** 하루에 몇
번이든 세션을 다시 할 수 있습니다(같은 날 재완료 409에 막히지 않습니다).

---

## 보안 설계

개인 기기를 인터넷에 노출하는 프로젝트인 만큼, 배포 전 체크리스트를 통과한 뒤에만 터널을 열도록 설계했습니다.

- **호스트 포트 매핑 0개** — 컨테이너는 전용 네트워크 안에서만 통신, 외부 노출은 터널 하나로 한정
- **Docker 소켓 미마운트 · privileged 미사용**
- **비-root 실행 (1000:1000) · `cap_drop: ALL` · `no-new-privileges` · `read_only`**
- **비밀값은 `.env`로 분리**하여 이미지·저장소에 포함하지 않음
- **Cloudflare Access**로 지정한 사용자만 접근 허용
- **정답·난이도 로직은 서버에만** 존재 (프런트에 정답을 내려보내지 않음)

자세한 위협 모델과 판단 근거는 `docs/`를 참고하세요.

---

## 문서

| 파일 | 내용 |
|------|------|
| `docs/prd.md` | 제품 요구사항 (무엇을, 누구를 위해) |
| `docs/threat-model.md` | 위협 모델 · 배포 전 보안 체크리스트 |
| `docs/api-spec.md` | API 명세 (엔드포인트 계약) |
| `docs/db-schema.md` | 데이터베이스 스키마 |
| `docs/decisions.md` | 설계 결정 기록 (임시조치와 해소 이력 포함) |
| `docs/week5-memory-design.md` | 기억력 과제 설계 근거 (기법 조사 · 왜 단서 회상인가) |
| `docs/handoff.md` | 기기 간 인계 메모 · **배포 현황** — 작업을 재개할 때 먼저 읽는 문서 |

---

## 배운 것

이 프로젝트는 코드를 빨리 짜는 것보다 **개념을 이해하고 검증하는 것**에 무게를 두었습니다.

- 문서를 먼저 쓰고(PRD·위협모델·API명세), 코드는 그 계약을 따르게 했습니다.
- 각 단계마다 "됐겠지"가 아니라 **실제로 확인**했습니다 — 커밋 전 실측, 재시작 후에도 데이터가 남는지, 호스트에 포트가 안 열렸는지, 인증이 실제로 막는지.
- 임시로 처리한 것은 결정 기록에 남겨두었다가 **반드시 되돌아와 해소**했습니다.

설계 판단의 근거와 임시조치·해소 이력은 `docs/decisions.md`에 단계별로 남겼습니다.

---

*이 프로젝트는 학습 목적의 개인 프로젝트입니다.*
